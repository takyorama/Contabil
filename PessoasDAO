#include "Pessoa.h"
#include <string.h>

class PessoaDAO{
  public:
    PessoaDAO();
    PessoaDAO(int, std::string, std::string, std::string, std::string, std::string, char *, char *, int);
    ~PessoaSAO();
    
    /********** Eventos  **********/
    Pessoa Find(std::string);
    int Insert(Pessoa &);
    int Change(Pessoa &);
    int Erase(Pessoa &);
    std::string Error();
  private:
    std::string erro;
    char mensagem[255];
};
